// Archivo JavaScript principal para la aplicación de alfabetización
document.addEventListener('DOMContentLoaded', function() {
    console.log('Aplicación de alfabetización cargada correctamente');
    
    // Animación de elementos con la clase float-animation
    const floatElements = document.querySelectorAll('.float-animation');
    if (floatElements.length > 0) {
        // La animación ya está definida en CSS
        console.log('Elementos animados inicializados');
    }
});
